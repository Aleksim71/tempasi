diff --git a/artifacts/notes/analytics_impl_notes.md b/artifacts/notes/analytics_impl_notes.md
new file mode 100644
index 0000000..78c4ef8
--- /dev/null
+++ b/artifacts/notes/analytics_impl_notes.md
@@ -0,0 +1,3 @@
+# Analytics v1 Implementation Notes (Cabinet → My Templates)
+
+Recreated placeholder: original implementation notes file is not present in this workspace snapshot.
diff --git a/artifacts/notes/analytics_schema_notes.md b/artifacts/notes/analytics_schema_notes.md
new file mode 100644
index 0000000..2ab8641
--- /dev/null
+++ b/artifacts/notes/analytics_schema_notes.md
@@ -0,0 +1,3 @@
+# Analytics Schema Notes (Cabinet → My Templates)
+
+Recreated placeholder: original notes file is not present in this workspace snapshot.
diff --git a/public/css/pages/cabinet.css b/public/css/pages/cabinet.css
index 8502d8d..cd06975 100644
--- a/public/css/pages/cabinet.css
+++ b/public/css/pages/cabinet.css
@@ -127,3 +127,37 @@
   gap: 10px;
   flex-wrap: wrap;
 }
+
+.mt-analytics {
+  width: 100%;
+  border-collapse: collapse;
+  margin-top: 16px;
+  font-size: 14px;
+}
+
+.mt-analytics th,
+.mt-analytics td {
+  padding: 8px 10px;
+  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
+  text-align: left;
+}
+
+.mt-analytics__sortLink {
+  color: inherit;
+  text-decoration: none;
+  opacity: 0.85;
+}
+
+.mt-analytics__sortLink.is-active {
+  font-weight: 600;
+  opacity: 1;
+}
+
+.mt-analytics__templateTitle {
+  font-weight: 600;
+}
+
+.mt-analytics__templateSlug {
+  font-size: 12px;
+  opacity: 0.8;
+}
diff --git a/src/web/modules/analytics/analytics.cabinet.service.cjs b/src/web/modules/analytics/analytics.cabinet.service.cjs
new file mode 100644
index 0000000..17b5bbc
--- /dev/null
+++ b/src/web/modules/analytics/analytics.cabinet.service.cjs
@@ -0,0 +1,107 @@
+/* eslint-env node */
+'use strict';
+
+const { getPool } = require('../../../../scripts/db.pool.cjs');
+
+const SORT_WHITELIST = new Set([
+  'created_at',
+  'deleted_at',
+  'rent_count',
+  'rent_revenue',
+  'buy_revenue',
+  'total_revenue',
+  'last_order_at',
+]);
+
+function normalizeSort(sortRaw) {
+  const s = typeof sortRaw === 'string' ? sortRaw.trim() : '';
+  if (SORT_WHITELIST.has(s)) return s;
+  return 'total_revenue';
+}
+
+function normalizeDir(dirRaw) {
+  const d = typeof dirRaw === 'string' ? dirRaw.trim().toLowerCase() : '';
+  return d === 'asc' ? 'asc' : 'desc';
+}
+
+function getOrderByClause(sort, dir) {
+  const orderDir = normalizeDir(dir);
+
+  switch (normalizeSort(sort)) {
+    case 'created_at':
+      return `st.created_at ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'deleted_at':
+      return `st.deleted_at ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'rent_count':
+      return `rent_count ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'rent_revenue':
+      return `rent_revenue_cents ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'buy_revenue':
+      return `buy_revenue_cents ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'last_order_at':
+      return `last_order_at ${orderDir} NULLS LAST, st.id ${orderDir}`;
+    case 'total_revenue':
+    default:
+      return `total_revenue_cents ${orderDir} NULLS LAST, st.id ${orderDir}`;
+  }
+}
+
+function formatMoneyEurFromCents(cents) {
+  if (cents === null || cents === undefined) return '0.00';
+  const n = Number(cents);
+  if (!Number.isFinite(n)) return '0.00';
+  return (n / 100).toFixed(2);
+}
+
+async function getMyTemplatesAnalytics({ ownerUserId, sort, dir }) {
+  if (!ownerUserId) {
+    throw new Error('OWNER_USER_ID_REQUIRED');
+  }
+
+  const pool = getPool();
+  const orderBy = getOrderByClause(sort, dir);
+
+  const sql = `
+    SELECT
+      st.id,
+      st.title,
+      st.slug,
+      st.created_at,
+      st.deleted_at,
+      MIN(o.created_at) FILTER (WHERE o.status = 'paid') AS first_order_at,
+      MAX(o.created_at) FILTER (WHERE o.status = 'paid') AS last_order_at,
+      COUNT(*) FILTER (WHERE o.status = 'paid' AND o.deal_type = 'RENT') AS rent_count,
+      COALESCE(SUM(o.amount_cents) FILTER (WHERE o.status = 'paid' AND o.deal_type = 'RENT'), 0) AS rent_revenue_cents,
+      COALESCE(SUM(o.amount_cents) FILTER (WHERE o.status = 'paid' AND o.deal_type = 'BUY'), 0) AS buy_revenue_cents,
+      COALESCE(SUM(o.amount_cents) FILTER (WHERE o.status = 'paid'), 0) AS total_revenue_cents
+    FROM seller_templates st
+    LEFT JOIN orders o
+      ON o.template_slug = st.slug
+    WHERE st.owner_user_id = $1
+    GROUP BY st.id
+    ORDER BY ${orderBy}
+  `;
+
+  const { rows } = await pool.query(sql, [ownerUserId]);
+
+  return (rows || []).map((r) => ({
+    title: r.title,
+    slug: r.slug,
+    created_at: r.created_at || null,
+    deleted_at: r.deleted_at || null,
+    first_order_at: r.first_order_at || null,
+    last_order_at: r.last_order_at || null,
+    rent_count: Number(r.rent_count || 0),
+    rent_revenue_cents: Number(r.rent_revenue_cents || 0),
+    buy_revenue_cents: Number(r.buy_revenue_cents || 0),
+    total_revenue_cents: Number(r.total_revenue_cents || 0),
+    rent_revenue_eur: formatMoneyEurFromCents(r.rent_revenue_cents),
+    buy_revenue_eur: formatMoneyEurFromCents(r.buy_revenue_cents),
+    total_revenue_eur: formatMoneyEurFromCents(r.total_revenue_cents),
+  }));
+}
+
+module.exports = {
+  getMyTemplatesAnalytics,
+};
+
diff --git a/src/web/routes/cabinet.pages.routes.cjs b/src/web/routes/cabinet.pages.routes.cjs
index 224d70b..02e993c 100644
--- a/src/web/routes/cabinet.pages.routes.cjs
+++ b/src/web/routes/cabinet.pages.routes.cjs
@@ -8,6 +8,7 @@ const multer = require('multer');
 
 const sellerTemplatesService = require('../../modules/templates/sellerTemplates.service.cjs');
 const sellerTemplatesRepo = require('../../modules/templates/sellerTemplates.repo.cjs');
+const analyticsService = require('../modules/analytics/analytics.cabinet.service.cjs');
 
 const { getPool } = require('../../../scripts/db.pool.cjs');
 
@@ -462,22 +463,47 @@ function createCabinetPagesRouter() {
   });
 
   router.get('/my-templates/analytics', (req, res) => {
-    res.render('pages/cabinet', {
-      activeSpace: 'my-templates',
+    const ownerUserId = getUserId(req);
+    if (!ownerUserId) return res.redirect('/login');
 
-      isMyTemplatesList: false,
-      isMyTemplatesAdd: false,
-      isMyTemplatesAnalytics: true,
-      isMyTemplatesEdit: false,
+    const sort = String(req.query.sort || '').trim() || undefined;
+    const dir = String(req.query.dir || '').trim() || undefined;
 
-      workspaceData: { items: [] },
-      workspaceError: null,
-
-      pageTitle: 'Analytics',
-      pageSubtitle: 'Sales, rentals and performance.',
-      panelTitle: 'Analytics',
-      panelText: '',
-    });
+    let workspaceError = null;
+    let rows = [];
+
+    analyticsService
+      .getMyTemplatesAnalytics({ ownerUserId, sort, dir })
+      .then((result) => {
+        rows = Array.isArray(result) ? result : [];
+      })
+      .catch((e) => {
+        workspaceError = e;
+      })
+      .finally(() => {
+        res.render('pages/cabinet', {
+          activeSpace: 'my-templates',
+
+          isMyTemplatesList: false,
+          isMyTemplatesAdd: false,
+          isMyTemplatesAnalytics: true,
+          isMyTemplatesEdit: false,
+
+          workspaceData: {
+            analytics: {
+              items: rows,
+              sort: sort || 'total_revenue',
+              dir: dir === 'asc' ? 'asc' : 'desc',
+            },
+          },
+          workspaceError,
+
+          pageTitle: 'Analytics',
+          pageSubtitle: 'Sales, rentals and performance.',
+          panelTitle: 'Analytics',
+          panelText: '',
+        });
+      });
   });
 
   // =========================
diff --git a/src/web/views/partials/space-my-templates.hbs b/src/web/views/partials/space-my-templates.hbs
index 4ee9782..2ab6803 100644
--- a/src/web/views/partials/space-my-templates.hbs
+++ b/src/web/views/partials/space-my-templates.hbs
@@ -262,9 +262,102 @@
   {{#if isMyTemplatesAnalytics}}
     <div class="cabinet__panel">
       <div class="cabinet__panelTitle">Analytics</div>
-      <p class="cabinet__panelText">
-        Sales / rentals analytics will be wired later (MVP).
-      </p>
+
+      {{#if workspaceData.analytics.items.length}}
+        <table class="mt-analytics">
+          <thead>
+            <tr>
+              <th>Template</th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=created_at&dir={{#if (and (eq workspaceData.analytics.sort 'created_at') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'created_at')}}is-active{{/if}}"
+                >
+                  Created
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=deleted_at&dir={{#if (and (eq workspaceData.analytics.sort 'deleted_at') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'deleted_at')}}is-active{{/if}}"
+                >
+                  Archived
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=created_at&dir={{#if (and (eq workspaceData.analytics.sort 'created_at') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'created_at')}}is-active{{/if}}"
+                >
+                  First order
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=last_order_at&dir={{#if (and (eq workspaceData.analytics.sort 'last_order_at') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'last_order_at')}}is-active{{/if}}"
+                >
+                  Last order
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=rent_count&dir={{#if (and (eq workspaceData.analytics.sort 'rent_count') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'rent_count')}}is-active{{/if}}"
+                >
+                  Rent count
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=rent_revenue&dir={{#if (and (eq workspaceData.analytics.sort 'rent_revenue') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'rent_revenue')}}is-active{{/if}}"
+                >
+                  Rent revenue
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=buy_revenue&dir={{#if (and (eq workspaceData.analytics.sort 'buy_revenue') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'buy_revenue')}}is-active{{/if}}"
+                >
+                  Buy revenue
+                </a>
+              </th>
+              <th>
+                <a
+                  href="/cabinet/my-templates/analytics?sort=total_revenue&dir={{#if (and (eq workspaceData.analytics.sort 'total_revenue') (eq workspaceData.analytics.dir 'desc'))}}asc{{else}}desc{{/if}}"
+                  class="mt-analytics__sortLink {{#if (eq workspaceData.analytics.sort 'total_revenue')}}is-active{{/if}}"
+                >
+                  Total revenue
+                </a>
+              </th>
+            </tr>
+          </thead>
+          <tbody>
+            {{#each workspaceData.analytics.items}}
+              <tr>
+                <td>
+                  <div class="mt-analytics__templateTitle">{{title}}</div>
+                  <div class="mt-analytics__templateSlug">{{slug}}</div>
+                </td>
+                <td>{{#if created_at}}{{created_at}}{{else}}—{{/if}}</td>
+                <td>{{#if deleted_at}}{{deleted_at}}{{else}}—{{/if}}</td>
+                <td>{{#if first_order_at}}{{first_order_at}}{{else}}—{{/if}}</td>
+                <td>{{#if last_order_at}}{{last_order_at}}{{else}}—{{/if}}</td>
+                <td>{{rent_count}}</td>
+                <td>{{rent_revenue_eur}}</td>
+                <td>{{buy_revenue_eur}}</td>
+                <td>{{total_revenue_eur}}</td>
+              </tr>
+            {{/each}}
+          </tbody>
+        </table>
+      {{else}}
+        <p class="cabinet__panelText">
+          No data yet. When customers buy or rent your templates, analytics will appear here.
+        </p>
+      {{/if}}
     </div>
   {{/if}}
 
