{{!-- src/web/views/pages/login.hbs --}}
{{#*inline "_head"}}
<link rel="stylesheet" href="/css/pages/auth.css" />
{{/inline}}

<main class="auth-page">
  <div class="auth-wrap">
    <section class="auth-card" aria-labelledby="auth-title">
      <header class="auth-head">
        <h1 class="auth-title" id="auth-title">Sign in</h1>
      </header>

      {{#if error}}
        <div class="auth-alert" role="alert">{{error}}</div>
      {{/if}}

      <form class="auth-form" method="post" action="/login" novalidate>
        <div class="auth-field">
          <label class="auth-label" for="email">Email</label>

          <div class="auth-control">
            <input
              class="auth-input"
              id="email"
              name="email"
              type="email"
              inputmode="email"
              autocomplete="email"
              placeholder="you@example.com"
              value="{{form.email}}"
              aria-invalid="{{#if fieldErrors.email}}true{{else}}false{{/if}}"
              required
            />
          </div>

          {{#if fieldErrors.email}}
            <div class="auth-hint auth-hint--error">{{fieldErrors.email}}</div>
          {{else}}
            <div class="auth-hint">Enter your email.</div>
          {{/if}}
        </div>

        <div class="auth-field">
          <label class="auth-label" for="password">Password</label>

          <div class="auth-control">
            <input
              class="auth-input auth-password"
              id="password"
              name="password"
              type="password"
              autocomplete="current-password"
              placeholder="••••••••"
              aria-invalid="{{#if fieldErrors.password}}true{{else}}false{{/if}}"
              minlength="6"
              required
            />

            <button class="auth-toggle" type="button" aria-label="Show password" data-auth-toggle="password">
              👁
            </button>
          </div>

          {{#if fieldErrors.password}}
            <div class="auth-hint auth-hint--error">{{fieldErrors.password}}</div>
          {{else}}
            <div class="auth-hint">At least 6 characters.</div>
          {{/if}}
        </div>

        <div class="auth-row">
          <label class="auth-check">
            <input type="checkbox" name="remember" value="1" {{#if form.remember}}checked{{/if}}>
            Remember me
          </label>

          <div class="auth-links" style="margin:0;">
            <span>No account?</span>
            <a href="/register">Create one</a>
          </div>
        </div>

        <div class="auth-actions">
          <button class="auth-btn" type="submit">Sign in</button>
        </div>
      </form>
    </section>
  </div>

  <footer class="auth-footer">
    <span>© Tempasi</span>
    <a href="/terms">Terms</a>
    <a href="/privacy">Privacy</a>
    <a href="/support">Support</a>
  </footer>
</main>

<script>
(() => {
  const btn = document.querySelector('[data-auth-toggle="password"]');
  const input = document.getElementById('password');
  if (!btn || !input) return;

  btn.addEventListener('click', () => {
    const isPass = input.type === 'password';
    input.type = isPass ? 'text' : 'password';
    btn.setAttribute('aria-label', isPass ? 'Hide password' : 'Show password');
  });
})();
</script>
