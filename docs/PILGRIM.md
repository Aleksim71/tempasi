# PILGRIM — Tempasi Admin Panel

Файл-передатчик состояния между чатами с Claude. Загружается в начале
каждого чата, обновлённая версия отдаётся в конце.

> **Текущий фокус:** Этап 1 закрыт. Этап 2: своя шапка + 3 заглушки,
> перевод дашборда, размер числа дельты — всё закоммичено. **Templates
> (read-only список + фильтры + пагинация) сделан, применён, закоммичен
> (`2ed6e94`)** — 7 коммитов впереди origin, не запушено. Следующий
> шаг по Templates — Патч 2: миграция `admin_blocked_at` + кнопка
> block/unblock + guard в `sellerTemplates.service.cjs` (запрет
> owner'у самому себя разблокировать) + сообщение в кабинете продавца.
> Либо — Users/Finance, если пользователь решит порядок сменить.

---

## Часть 1. План работ

- [x] **Этап 0 — Анализ кодовой базы.** Стек: Node.js/Express 5, PostgreSQL,
      Handlebars SSR. Сущности: users, seller_templates, orders, entitlements,
      cases, account_credits/account_credit_usages, cart_items. Роль `role` у
      users уже существовала, но не использовалась для контроля доступа.
- [x] **Этап 1 — Фундамент админки.** Применено и подтверждено 2026-07-17
      (скриншот `/admin?period=28` — дашборд рендерится, header/footer как
      в магазине, KPI считаются). ⚠️ Ранее в этом файле ошибочно стоял [x]
      раньше срока (2026-07-10) — код step1 существовал только как
      непримененный zip. Урок: не отмечать чекбокс без факта применения.
  - [x] Миграция `20260716_0001_admin_role_and_audit_log.sql`: CHECK на
        `users.role` (`user|seller|admin|superadmin`) + таблица
        `admin_audit_log`. Применена через `run.sh` 2026-07-17, бэкап
        `users` сделан автоматически.
  - [x] `require-admin.web.js` — ESM guard для `/admin/*`.
  - [x] `admin.pages.routes.cjs` — дашборд `/admin`.
  - [x] `dashboard.hbs`, `403.hbs`.
  - [x] `public/css/pages/admin.css`.
  - [x] `app.js` — монтирование `/admin`.
  - [x] `auth.pages.routes.js` — редирект admin/superadmin на `/admin`.
  - [x] Применить `tempasi-admin-step1-v2.zip` — подтверждено (`apply_
    20260717_071617.log`, миграция и code-diff применились чисто).
  - [x] Назначить первого admin (`admin@tempasi.com`) — подтверждено
        косвенно: скриншот дашборда снят с залогиненным доступом к `/admin`.
  - [x] **Регресс, найден и исправлен 2026-07-17:** `npm test` показал 2
        падающих интеграционных теста (`creditLedger.integration.test.cjs`,
        `failedCancelledExpiredRentCheckout.integration.test.cjs`) —
        они вставляли тестового пользователя с `role='observer'`,
        значением вне нового CHECK. Решение (выбор пользователя): не
        расширять CHECK, а переписать эти 2 теста на `role='user'`.
        Хотфикс передан как `tempasi-admin-step1-hotfix-observer-role.zip`
        (только код тестов, миграций/БД не трогает). **Подтверждено
        2026-07-18: `npm test` зелёный** (58/59 suites, 1 skipped;
        176/177 tests, 1 skipped — пропуск сторонний, не связан с
        хотфиксом). Этап 1 закрыт полностью.
        Урок на будущее: перед CHECK-констрейнтами на существующую
        колонку грепать реальные значения не только в `src/`, но и во
        всём `tests/` — иначе легко сломать чужие фикстуры.
- [ ] **Этап 2 — Модерация шаблонов** (`seller_templates`): список,
      approve/reject/unpublish, просмотр preview/demo_url, ручная правка цен.
      **Важно:** сейчас у `seller_templates.status` только два значения —
      `draft`/`published`, статуса "на модерации" не существует, продавец
      публикует сам, без какого-либо admin-гейта. Этап 2 должен либо
      добавить промежуточный статус (например, `pending`), либо оставить
      постмодерацию (unpublish уже опубликованного). Решение не принято.
  - [x] **Своя шапка админки** (2026-07-18, подтверждено `npm test`):
        `Tempasi` + бейдж `ADMIN` слева (лого/текст — ссылка на
        публичный `/templates`), по центру `Dashboard/Templates/Users/
    Finance` с подсветкой активного пункта, справа только Logout
        (Cart и Cabinet убраны). **Решение изменено против записи от
        2026-07-16:** шапка админки теперь СВОЯ (`admin-header.hbs`),
        визуально скопирована с публичной, но не расшарена — публичный
        `site-header.hbs` не тронут. Футер остаётся общим (не менялось).
        Новый layout-switch в `main.hbs`: `isAdmin` → `admin-header`,
        иначе `site-header`.
  - [x] **3 пустые страницы-заглушки** под пункты меню: `/admin/templates`,
        `/admin/users`, `/admin/finance` ("coming soon"), защищены тем
        же `requireAdminWeb` (гард монтируется на `/admin` целиком в
        `app.js`, доп. правок там не понадобилось). Наполнение —
        отдельными шагами позже.
  - [x] **Хотфикс (2026-07-18):** `hbs.registerPartials(dir)` в этом
        проекте ненадёжен (уже задокументировано в самом коде —
        поэтому критичные партиалы регистрируются вручную). Новый
        `admin-header` не попал в этот ручной список → ошибка
        "The partial admin-header could not be found". Исправлено в
        `src/app.web.js` — добавлен по образцу `site-header`.
        **Урок на будущее:** при добавлении любого нового партиала в
        `views/partials/*` в этом проекте — сразу проверять, зарегистрирован
        ли он вручную в `app.web.js` (список `pXxx`/`registerPartialSafe`),
        не полагаясь на автосканирование.
        **Не закоммичено на конец чата** — пользователь коммитит сам
        локально, следующий чат должен спросить, что уже в git.
  - [x] **Templates: список + фильтры (read-only), 2026-07-18,
        коммит `2ed6e94`.** Фильтры: даты регистрации/изменения
        (диапазон), каталог (`category`, реальные значения из БД),
        статус (`Published/Draft/All` — без "Заблокированные", см.
        ниже), статус продажи (`Sold/Not sold/All`), поиск по
        продавцу (email/имя), поиск по названию/slug. Таблица:
        превью (с фоллбеком-рамкой при отсутствии/битой картинке),
        название, даты, цена продажи/аренды (`—` если `NULL`),
        продавец (`nickname → full_name → email`), статус, статус
        продажи. Пагинация 25/страница.
        **"Продан" переиспользует существующий бизнес-инвариант**
        `TEMPASI_STEP_6D_BUY_EXCLUSIVITY_GUARD`
        (`orders.service.cjs`/`hasPaidBuyByTemplateSlug`) — шаблон
        эксклюзивный, продаётся только 1 раз, это уже enforced на
        checkout, не гипотеза.
        **Схема подтверждена на проде (`\d`):** `seller_templates.category`
        и таблица `user_profiles` реально существуют (тот же класс
        риска, что и `users.role` в Этапе 1 — колонки/таблицы есть
        в `sql/migrations/`+тестовой схеме, но отсутствуют в
        "канонической" `src/db/migrations/"; раннера миграций в
    проекте нет вообще, npm-скрипта `migrate`не существует).
    **Урок на будущее (CSS):** скопировал разметку с
   `credit-ledger.hbs`, но `.card`/`.table-card`/`.button`/
    `.status-pill`/`.empty-state` там либо мертвы (`custom.css`    нигде не подключён), либо живут в`cabinet-finance.css`
    (грузится только на cabinet-страницах) — на админке было бы
    без стилей. Перед копированием чужой разметки в новый раздел
    — всегда проверять, откуда реально берётся стилизация класса
    (`grep`по`public/css/`), не полагаться на визуальное сходство.
    Использованы реальные глобальные классы (`.c-btn`, `.input`,
    `.select`, `.form\_\_label`) + свои `admin-\*`в`admin.css`.
    Отдельным мини-патчем (тот же коммит) поправлен фоллбек
    превью: рамка теперь отдельный persistent `<span>`, картинка —
    вложенный слой с `onerror="this.remove()"` (раньше битая
        картинка не показывала плейсхолдер).
  - [ ] **Блокировка шаблонов — решение принято, не реализовано:**
        `draft`/`published` — статусы, которые меняет сам продавец.
        Block/unblock — отдельное действие ТОЛЬКО админа, добавляет
        `admin_blocked_at TIMESTAMPTZ` (по аналогии с `deleted_at`),
        не трогая CHECK на `status`. Block: `status` форсится в
        `draft` + `admin_blocked_at = NOW()`. Unblock: `admin_blocked_at
    = NULL`, `status` ОСТАЁТСЯ `draft` (не восстанавливается в
        `published` автоматически — продавец публикует заново сам).
        Продавцу в кабинете при блокировке показывается сообщение
        "заблокировано админом" вместо обычного draft-стейта.
        **Важная дыра, которую нашёл сам при сверке с кодом (не была
        очевидна из формулировки пользователя):** без доработки
        `updateStatusByOwner()` в `sellerTemplates.repo.cjs` продавец
        может тривиально снять блокировку, просто нажав "Опубликовать"
        ещё раз. Нужен guard: если `admin_blocked_at IS NOT NULL` —
        запрет перехода в `published`, ошибка с понятным текстом.
        По правилам изоляции: чтение (список+фильтры) — обычный SQL
        в самом admin-модуле (ОК, уже так и сделано). Запись
        (block/unblock) — обязана идти через новый метод в
        `sellerTemplates.service.cjs`/`repo.cjs`, не raw SQL в admin.
        Бонус: публичный каталог фильтрует `status='published'`,
        так что блокировка (форс в draft) автоматически прячет
        шаблон из каталога — отдельно каталожный запрос трогать
        не придётся. Этот кусок трогает чужой модуль
        (`sellerTemplates.*`) и вьюху кабинета продавца — не просто
        "ещё одна кнопка в admin-таблице".
- [ ] **Этап 3 — Управление заказами** (`orders`): список с фильтрами по
      статусу/провайдеру, ручной refund/cancel, сверка с провайдером оплаты.
- [ ] **Этап 4 — Управление пользователями**: список/поиск, смена роли,
      блокировка, принудительный logout, сброс email_verified.
- [ ] **Этап 5 — Финансовый леджер** (`account_credits`): просмотр леджера
      по пользователю, ручное начисление/аннулирование кредита.
- [ ] **Сквозная задача (всё время):** каждое мутирующее действие в админке
      пишется в `admin_audit_log`. Пока не используется — в Этапе 1 таблица
      только создаётся, писать в неё начнёт Этап 2 (первые мутации).

---

## Часть 1.1. Спецификация дашборда `/admin` (согласовано 2026-07-16)

4 KPI-карточки + переключатель периода (1 / 7 / 28 дней, `?period=`):

1. **Пользователи** — общее количество (все, `COUNT(*) FROM users`) +
   дельта = нетто-изменение за период (`created_at` в пределах периода;
   у users нет мягкого удаления, поэтому дельта фактически = кол-во новых
   регистраций, всегда ≥ 0).
2. **Шаблоны** — общее количество **только `status='published'` и
   `deleted_at IS NULL`** (решение: считаем то, что видно в каталоге, не
   черновики) + дельта = нетто (вновь опубликованные минус удалённые за
   период). **Известное ограничение:** переход `published -> draft` без
   удаления записи в дельте не отражается — в схеме нет истории статусов.
   Когда Этап 2 начнёт писать переходы в `admin_audit_log`, дельту можно
   будет считать точно.
3. **Выручка · аренда** (`orders WHERE deal_type='RENT' AND status='paid'`)
   — сумма за период + дельта **в деньгах** относительно предыдущего
   периода такой же длины.
4. **Выручка · продажа** (`deal_type='BUY'`) — аналогично.

Визуал: **полностью как в магазине** — те же компоненты, header/footer как
на публичных страницах (решение от 2026-07-16, отменяет более раннюю идею
"своя визуальная система"). Своя CSS только для карточек/переключателя
периода (`admin.css`), токены (цвета/шрифты/spacing) — из `core.css`.

**Побочная находка (не блокер Этапа 1):** `orders.amount_cents`,
используемый уже существующей аналитикой (`analytics.cabinet.service.cjs`)
и новым дашбордом, тоже добавлен только в `tests/helpers/migrateDb.cjs`, а
не в `src/db/migrations/`. Риск того же типа, что и с `users.role` — если
на проде колонки нет, аналитика уже должна была на это натыкаться. Не
трогаем в рамках Этапа 1, но стоит проверить при случае.

---

## Часть 2. Правила работы

### Режимы

- **Обсуждение** — просто разговариваем, ничего не меняем в проекте.
- **Работа** — берём одну маленькую цель из плана (Часть 1), доводим до
  конца по шагам ниже, проверяем, отмечаем чекбокс, только потом берём
  следующую.

### Порядок работы над одной целью (обязателен, без исключений)

1. **Сбор информации.** Прежде чем предлагать решение или менять
   код/БД/окружение, Claude собирает всё необходимое по факту (читает
   реальные файлы, миграции, схему БД, конвенции проекта) — не
   предполагает и не додумывает, как "должно быть устроено".
2. **Варианты решения.** На основе собранной информации Claude предлагает
   вариант(ы) решения с плюсами/минусами, привязанными к реальному коду
   (не общими рассуждениями).
3. **Обсуждение и выбор.** Обсуждаем, выбираем вариант вместе.
4. **Реализация.** Только после выбора — патч (файлы/SQL/тесты) в
   `incoming/`.
   Пропускать шаг 1 (сразу писать код по предположению) — нарушение
   процесса, даже если цель кажется тривиальной.

### Изоляция админки (MVP на том же сайте, но с заделом на отделение)

- Код/вьюхи — только в своих namespace'ах: `src/web/routes/admin.*.routes.cjs`,
  `src/web/middleware/require-admin.web.js`, `src/web/views/pages/admin/*`,
  `public/css/pages/admin*.css`.
- **Визуал — исключение из общей идеи изоляции:** header/footer/шрифты
  сознательно общие с публичным сайтом (решение 2026-07-16). Изоляция
  относится к коду/бизнес-логике, не к дизайну.
- **Админка не пишет ad-hoc SQL по таблицам, которыми владеют другие
  модули.** Если нужна операция (например, unpublish шаблона) — она
  добавляется как метод в сервис модуля-владельца (например,
  `sellerTemplates.service.cjs`), а не как отдельный запрос в
  admin-роуте. Бизнес-правила живут в одном месте. **(Начнёт
  действовать с Этапа 2 — в Этапе 1 админка только читает данные для
  дашборда.)**
- Известный заранее компромисс, который не решаем сейчас: авторизация
  админки использует общие `sessions`/`users`. При реальном отделении
  потребуется либо общий session-store (Redis), либо SSO — сейчас это
  не блокер, просто зафиксированный риск на будущее.

### Обмен файлами

```
incoming   <- патчи от Claude (в этом чате: /home/aleksim/Загрузки)
outgoing   <- результаты/логи от пользователя обратно Claude
             (в этом чате: /home/aleksim/workspace/projects/tempasi/Outbox)
archive    <- применённые патчи (история)
```

Проект: `/home/aleksim/tempasi`.

### Формат патча

- Входящий файл — zip, но **не обязательно Python**: это могут быть
  bash/PowerShell-команды, обычные diff/patch-файлы, `.sql`-файлы.
- Обязательная единая точка входа — `run.sh` (bash, под Ubuntu),
  которая применяет всё и пишет лог в `outgoing/`.
- Файловые изменения — предпочтительно как unified diff / `git apply`
  (если проект под git), а не спрятаны внутри произвольного скрипта.
- Изменения в БД — отдельный `.sql`-файл + шаг с бэкапом (`pg_dump`
  нужных таблиц) перед применением + явное подтверждение перед
  деструктивными операциями (UPDATE/DELETE/DROP).
- Тесты можно писать и прогонять как часть патча; вывод — в `outgoing/`.

**Апробировано в этом чате:** `run.sh` умеет откатываться безопасно —
если нет `DATABASE_URL`, нет `pg_dump`, или в `users` находятся роли вне
допустимого набора, миграция не применяется, code-diff при этом уже
может быть применён (он идёт первым и не зависит от БД).

### Правило "Пилигрима"

- Загружается в начале чата.
- Обновляется Claude в конце чата (после завершения задач/уточнения правил).
- Новый чат открывается: либо по достижении естественной точки паузы
  (закрыт этап/подзадача), либо ориентировочно через ~20 сообщений —
  пользователь отслеживает это сам, Claude не считает сообщения
  автоматически.

### Технические детали окружения

- ОС/шелл для запуска патчей: **Ubuntu, bash**.
- Проект под git: **да**, ветка `feat/analytics-kpi-summary`.
- Прямой доступ к БД с рабочей машины: **через `.env` (`DATABASE_URL`)**,
  без отдельно настроенного `psql`-клиента — значит SQL-патчи должны
  подключаться через `DATABASE_URL` из `.env` (например,
  `psql "$DATABASE_URL" -f patch.sql`, если `psql` всё же есть в системе,
  либо через `node`-скрипт на `pg`, который уже используется в проекте).

---

## Часть 3. Журнал изменений

- **2026-07-10** — Обсуждены варианты архитектуры (встроенная админка vs
  отдельный сайт), выбран Вариант 1. Реализован и доставлен Этап 1
  (`tempasi-admin-step1.zip`) — как позже выяснилось, этот патч так и не
  был применён к реальной рабочей копии.
- **2026-07-15** — Согласован процесс работы: режимы обсуждение/работа,
  обмен через `incoming/outgoing/archive`, формат патчей, файл `PILGRIM.md`
  как передатчик состояния между чатами.
- **2026-07-16** — Новый чат. Загружен реальный `tempasi.zip`, при анализе
  выяснилось: код Этапа 1 отсутствует в рабочей копии (ветка
  `feat/analytics-kpi-summary`, коммит `5aec575`), PILGRIM.md был неточен.
  Пользователь подтвердил: это снимок до Этапа 1, ничего не применялось.
  Пересобрали Этап 1 заново по новому протоколу (diff + run.sh).
  Пересмотрено содержание дашборда: 4 KPI (пользователи, published-шаблоны,
  выручка аренда, выручка продажа) с переключателем периода 1/7/28 дней и
  дельтой к предыдущему периоду (в деньгах для выручки). Решено: визуал
  админки — как на публичном сайте (header/footer/шрифты общие), а не
  отдельная система. Найдено и задокументировано: у `seller_templates`
  нет статуса "на модерации" (только draft/published) — влияет на дизайн
  Этапа 2; `orders.amount_cents` тоже не в `src/db/migrations/` (тот же
  тип риска, что и с `users.role`, но чужой/предсуществующий). Патч
  собран, синтаксис проверен (`node --check`, precompile `.hbs`),
  диф протестирован сухим прогоном `run.sh` на копии репозитория —
  применяется чисто. Передан как `tempasi-admin-step1-v2.zip` в
  `incoming/`.
- **2026-07-17** — `run.sh` отработал на реальном проекте: code-diff и
  SQL-миграция применились чисто (лог `apply_20260717_071617.log`, бэкап
  `users` подтверждает отсутствие admin/superadmin до миграции). Назначен
  первый admin (`admin@tempasi.com`), проверен вживую дашборд `/admin` —
  всё как задумано (скриншот). Прогон `npm test` выявил регресс: 2
  интеграционных теста падали с `users_role_check`, потому что вставляли
  `role='observer'` — легитимное тестовое значение, про которое я не знал
  (грепал только `src/`, не весь `tests/`). Обсудили 2 варианта фикса
  (расширить CHECK vs переписать тесты), пользователь выбрал переписать
  тесты на `role='user'`. Хотфикс
  (`tempasi-admin-step1-hotfix-observer-role.zip`, только 2 тестовых
  файла, без изменений схемы) собран, диф проверен сухим прогоном,
  передан в `incoming/`. Этап 1 отмечен закрытым; Этап 2 не начат.
- **2026-07-18** — Пользователь подтвердил применение хотфикса: `npm
test` зелёный (58/59 suites passed, 1 skipped; 176/177 tests passed,
  1 skipped). Этап 1 подтверждён закрытым полностью, без открытых
  хвостов. Начинаем обсуждение Этапа 2 — пользователь описывает
  остальные страницы админки.
- **2026-07-18 (продолжение)** — Согласована шапка админки: слева
  `Tempasi`+`ADMIN`-бейдж (ссылка на публичный сайт), центр
  `Dashboard/Templates/Users/Finance` с подсветкой активного пункта,
  справа только Logout (Cart и Cabinet убраны). Решение: своя шапка
  (не расшаренная с публичной), визуально скопированная; футер
  общий. Собран и передан патч `tempasi-admin-step2-header-and-stubs.zip`
  (новый `admin-header.hbs`, ветка в `main.hbs` по `isAdmin`, 3
  роута-заглушки `/admin/templates`|`/admin/users`|`/admin/finance` +
  их вьюхи, стили в `admin.css`). После применения — ошибка "The
  partial admin-header could not be found": `hbs.registerPartials(dir)`
  в проекте ненадёжен, партиалы регистрируются вручную в `app.web.js`,
  `admin-header` в список не попал. Собран и передан хотфикс
  `tempasi-admin-step2-hotfix-header-partial-registration.zip`
  (правка `app.web.js`, по образцу `site-header`). Пользователь
  применил оба патча, перезапустил сервер, подтвердил: `/admin`
  рендерится, `npm test` зелёный (58/59, 176/177 — тот же посторонний
  skip). Пользователь коммитит изменения локально сам; не закоммичено
  на момент конца чата.
- **2026-07-18 (продолжение 2)** — Пользователь закоммитил всё
  накопленное четырьмя коммитами: `757b7d7` (шапка+заглушки),
  `631e68c` (фундамент Этапа 1), `7c84fac` (хотфикс `role='observer'`
  -> `role='user'`, наше, просто раньше не попало в коммит), `09c983c`
  (файлы вне админки — не проверялись построчно, закоммичены как есть
  по просьбе пользователя). Рабочее дерево чистое, 4 коммита впереди
  `origin/feat/analytics-kpi-summary`, не запушено.
- **2026-07-18 (продолжение 3)** — По запросу: перевод дашборда на
  английский + число в дельте увеличено до размера базового числа
  (32px, вынесено в свой `<span class="admin-kpi-delta__value">`).
  Патч `tempasi-admin-step2-dashboard-english-and-delta-size.zip`
  (3 файла: `admin.pages.routes.cjs`, `dashboard.hbs`, `admin.css`)
  собран, применён, `npm test` зелёный (58/59, 176/177). Коммит —
  на пользователе, не подтверждён в чате на момент записи.

---

## Часть 4. Открытые риски и принятые решения

- **Решение:** админка встроена в существующее приложение (роль
  `admin`/`superadmin` в общей таблице `users`), а не отдельный сайт —
  из-за общих таблиц `users`/`sessions` и стадии MVP.
- **Решение (2026-07-16):** визуал админки полностью переиспользует
  публичный дизайн (header/footer/шрифты), а не строится как отдельная
  визуальная система. Изоляция остаётся только на уровне кода/данных.
- **Риск:** таблица `users` не создаётся ни одной миграцией в
  `src/db/migrations/` — её схема известна только из
  `tests/helpers/migrateDb.cjs` (тестовая БД). Migration `run.sh`
  подстраховывается (`ADD COLUMN IF NOT EXISTS role`) и проверяет
  недопустимые значения role перед наложением CHECK, но сам факт
  отсутствия миграции для базовой таблицы остаётся риском.
- **Риск (найден 2026-07-16, не наш):** `orders.amount_cents` в том же
  положении, что и `users.role` — используется в проде (аналитика,
  теперь и дашборд), но нет миграции в `src/db/migrations/`.
- **Открытый вопрос для Этапа 2:** у `seller_templates` нет состояния
  "на модерации" — решить, добавлять ли `pending`-статус или делать
  постмодерацию поверх `draft`/`published`.
- **Подтверждено (2026-07-18):** `tempasi-admin-step1-v2.zip` и хотфикс
  `tempasi-admin-step1-hotfix-observer-role.zip` применены, первый
  admin назначен, `npm test` зелёный. Этап 1 закрыт без открытых
  хвостов.
- **Закоммичено (2026-07-18), 7 коммитов впереди
  `origin/feat/analytics-kpi-summary` (не запушено):**
  - `757b7d7` — шапка+заглушки Этапа 2 (9 файлов).
  - `631e68c` — фундамент Этапа 1: миграция, guard, mount `/admin`,
    dashboard, 403, редирект логина (6 файлов).
  - `7c84fac` — хотфикс Этапа 1: `role='observer'` -> `role='user'`
    в 2 тестовых файлах (наше, просто раньше не попало в коммит).
  - `09c983c` — "wip: pending template/demo/cabinet changes" (8 файлов
    вне админки — `template-demo.*`, `cabinet.pages.routes.cjs`,
    `templates.routes.js`, `template.hbs`, `template-details.hbs`,
    `space-cases.hbs`, `template-card.v2.hbs`; не проверялось построчно,
    закоммичено как есть по просьбе пользователя).
  - `a1b9764` — перевод дашборда на английский + размер числа дельты
    (3 файла: `admin.pages.routes.cjs`, `dashboard.hbs`, `admin.css`).
  - - 1 коммит (хэш не подтверждён в чате) — `docs/PILGRIM.md`
      добавлен в git. **Решение (2026-07-18): `docs/PILGRIM.md` теперь
      трекается в git** — включать его в коммиты вместе с рабочими
      файлами, а не оставлять untracked.
  - `2ed6e94` — Templates: список + фильтры + пагинация + фикс
    фоллбека превью (3 файла: `admin.pages.routes.cjs`,
    `templates.hbs`, `admin.css`).
- **Применено, но НЕ в списке коммитов выше (2026-07-18):** сама эта
  правка `docs/PILGRIM.md` — пользователь коммитит в конце чата,
  следующий чат должен спросить актуальный `git log`, не считать её
  автоматически закоммиченной.
